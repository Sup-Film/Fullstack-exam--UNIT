# Example .env file for Other Services
# Copy this file to .env and fill in the required values

# -- PostgreSQL Configuration --
POSTGRES_DB=live_order_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# -- Redis Configuration --
# (Add if needed)
